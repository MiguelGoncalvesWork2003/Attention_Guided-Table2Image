# Attention-Guided Tabular-to-Image Pipeline – Core Dependencies
#
# Install with:
#   pip install -r requirements.txt

# ------------------------------
# Core data & ML libraries
# ------------------------------
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
scipy>=1.7.0              # required by scikit-learn metrics

# ------------------------------
# Deep learning
# ------------------------------
torch>=1.9.0              # PyTorch (no strict upper bound, works with 2.x)
pytorch-tabnet>=4.0.0     # TabNet classifier (official PyTorch implementation)
xgboost
lightgbm
catboost

# ------------------------------
# Interactive dashboard & visualization
# ------------------------------
streamlit>=1.19.0
matplotlib>=3.5.0
seaborn>=0.11.0
Pillow>=9.0.0             # image handling (used internally by matplotlib/streamlit)

# ------------------------------
# Hyperparameter search (API)
# ------------------------------
optuna>=3.0.0             # Bayesian & random search

# ------------------------------
# Miscellaneous utilities
# ------------------------------
tqdm>=4.62.0              # progress bars (used by Optuna, training loops)